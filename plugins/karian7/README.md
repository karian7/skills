# karian7 plugin

Koasu(karian7) 의 통합 스킬 플러그인.

## 포함된 스킬

- **md-preview** — 로컬 Markdown 파일을 pandoc 으로 HTML 변환 후 브라우저에 띄우고 라이브 리로드

## 호출

- Claude Code: `/karian7:md-preview`
- Codex: skill list 에서 `karian7:md-preview`
